export enum NumberFactType {
  SPAM = 1, // спам
  FRAUD = 2, // мошенники
  MISSED_CALL = 3, // прозвон
  IMPORTANT_CALL = 4, // важный звонок
  OTHER = 5, // другое
}

export default NumberFactType;
